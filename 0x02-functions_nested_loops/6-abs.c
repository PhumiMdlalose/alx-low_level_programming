#include "main.h"

 /**
 * _abs - Computed the abs val of an int
 * @c: The number to be computed.
 * Return: Absolute value of the int.
 */

int _abs(int)
{
	if (c < 0)
	{
	int abs_val;

	abs_val = c * -1;
	return (abs_val);
	}
	return (c);
}
