#include "main.h"

/**
 * mul - Multiplies the two int
 * @x: The first numb to multiply
 * @y: The second numb to multiply
 * Return: The results of multiplication between x param and y param
 */

int mul(int x, int y)
{
	return (x * y);
}
